# --- Web framework -------------------------------------------------------
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
python-dotenv==1.0.1
gunicorn==21.2.0

# --- Database drivers ----------------------------------------------------
# Postgres is the default for hosted deploys (Render / Railway / Neon / Fly).
psycopg[binary]==3.2.3
# MySQL is still supported for local development against the original schema.
PyMySQL==1.1.1
cryptography==42.0.5
